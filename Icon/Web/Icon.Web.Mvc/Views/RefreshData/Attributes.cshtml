@model Icon.Web.Mvc.Models.RefreshData.RefreshAttributesViewModel

@{
    ViewBag.Title = "Refresh Attributes";
}

    <h2>Refresh Attributes</h2>


<div id="searchOptions" class="page-section">
        @using (Html.BeginForm("Attributes", "RefreshData", FormMethod.Post))
        {
            <div class="form-group">
                @Html.Label(String.Format("Attribute IDs (one per line, maximum {0})", 3000), new { @class = "form-label" })
                @Html.TextAreaFor(model => Model.AttributeIds, 15, 13, new { @class = "form-control form-input", @type = "number" })
                @Html.ValidationMessageFor(model => model.AttributeIds, "", new { @class = "text-danger" })
            </div>

            <div class="form-group">
                <input type="submit" value="Refresh" class="btn btn-light form-submit" id="refresh-button" />
            </div>
        }
    @if (ViewBag.Message != null)
    {
        <div>
            <p>@ViewBag.Message</p>
        </div>
    }
</div>
